// pai r square

#include<stdio.h>
int main()
{
  const float PI=3.14519;
  int radius;
  printf("Enter the radius of circle in cm:");
  scanf("%d",&radius);
  printf("Area of circle is %f",PI * radius * radius);       // pai r square
}